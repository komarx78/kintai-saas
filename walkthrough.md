# 👶 産前産後・育児休業 社員入力＆管理者審査パイプライン 組み込み完了報告

我が君より拝領した号令（「産休手続きを入社手続きのように社員に飛ばすようにしたい。社員に打ち込みしてもらって完了できるようにして」）に基づき、五頭体制（孔明・司馬懿・姜維・周瑜・荀彧）にて社員向け専用申請ステーション、管理者URL発行機能、提出書類審査連動、および公的A4原本自動反映を完走いたしました。

---

## 🏛️ 実装の概要

### 1. 社員向け 産休・育休申請ステーション（スマートフォン完全対応）
- **ファイル**: `src/pages/EmployeeMaternityApplication.tsx`
- **URL**: `/maternity/apply?tenant_id=...&user_id=...&name=...`
- **特徴**:
  - 新入社員の入社手続き（`/onboarding/welcome`）と同様、ログイン不要でも専用URLから即座にアクセス可能。
  - スマホ幅（375px〜420px）で直感的に操作できる4ステップUI（周瑜・モバイルゼロバグ設計）。
  - **Step 1: 出産予定日 ＆ 法定期間自動計算**: 出産予定日を入力すると、産前休業（単胎42日前/多胎98日前）、産後休業（56日後）、育児休業（原則1歳/1.5歳/2歳）、復職予定日が一撃で法令自動計算。
  - **Step 2: 連絡先 ＆ お子様の情報**: 休業中の電話番号・メール・LINE ID、お子様氏名・生年月日（出生後申請対応）。
  - **Step 3: 母子健康手帳 写真提出**: スマホカメラ等で撮影した母子手帳の表紙・分べん予定日記載ページを高画質軽量化（`compressImageFile`）して即座にアップロード。
  - **Step 4: 住民税立替精算希望 ＆ 電子署名**: 無給期間中の特別徴収住民税の精算方針（復職後給与天引き、毎月振込、復職時一括）を選択し、氏名署名で申請送信。

### 2. 管理者側「社員へ専用URLを飛ばす」導線の配備
- **ファイル**: `src/pages/OnboardingAdminDashboard.tsx` および `src/components/MaternityLeaveModal.tsx`
- **導線**:
  1. **手続きステーション内**: ヘッダーの「📱 社員にURLを送付」ボタンをクリックすると、ワンクリックで専用URLおよびLINE・メール用送信用定型文が生成・コピー可能。
  2. **📁 労務書類キャビネット内**: 「6. 👶 産前産後・育児休業」タブ内に「📱 社員に専用入力URLを送付」ボタンを配備。
  3. **定型文コピー**: 「〇〇様、産前産後休業・育児休業の手続きのため、以下の専用URLより入力をお願いします...」という実務定型文がワンクリックでクリップボードへコピー可能。

### 3. 管理者の「提出書類審査」ビューへの自動直結 ＆ ワンクリック承認
- 社員が送信すると、実DB（`employee_maternity_leaves`）に `status: 'submitted'` として保存されると同時に、`employee_document_submissions` に `document_type: 'maternity_leave'` として登録。
- 管理者画面の「未審査の提出書類」バッジが自動カウントアップし、審査カードに予定日・休業期間・復職日・住民税精算希望・添付写真プレビューが表示。
- 管理者が「承認」ボタンを押すと、ステータスが `approved` に確定し、大元の公的A4帳票原本4枚（産休申請書・育休申請書・チェックリスト・住民税立替表）に100%自動反映。

### 4. 社員マイページ（`/onboarding/my/*`）への導線統合
- **ファイル**: `src/pages/EmployeeOnboardingSubmission.tsx`
- ログイン中の社内従業員がマイページのタブから「👶 産前産後・育児休業申請」を直接開いて申請できる導線を増設。

---

## 🗄️ データベースマイグレーション

- **ファイル**: `add_maternity_leave_application_support.sql`
- **対象テーブル**: `public.employee_maternity_leaves`, `public.employee_document_submissions`
- **セキュリティ**:
  - `tenant_id` マルチテナント拘束
  - 完全冪等性構文（`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, `DROP POLICY IF EXISTS ... CREATE POLICY`）

---

## 🛡️ 五頭軍団 自律検証カルテ（全項目合格）

- [x] **構文・型エラーゼロ**: `tsc -b && vite build` 実行 ➔ エラー 0件・完全パス（姜維検証）
- [x] **スキーマ乖離ゼロ**: テーブルカラムと TypeScript 型定義が完全一致（司馬懿監査）
- [x] **SQL一撃無痛**: `ADD COLUMN IF NOT EXISTS`, `DROP POLICY IF EXISTS` 完備（司馬懿監査）
- [x] **実DB永続化**: `submitEmployeeMaternityApplication` による実DB Upsert連動（司馬懿実証）
- [x] **マルチテナント・他社漏洩遮断**: `tenant_id` 拘束およびRLSポリシー完備（司馬懿監査）
- [x] **原本完全一致**: 社員入力データが原本4枚のA4印刷プレビューへ100%自動反映（荀彧検証）
- [x] **既存デザイン不可侵**: 労務ダッシュボードやマイページの既存UIを1ミリも破壊せず調和（周瑜検証）
- [x] **現場スマホ実寸検証**: 幅375px〜420pxでの文字被り・横崩れゼロ（周瑜検証）
