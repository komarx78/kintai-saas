# 【完全整理】従業員メールアドレス登録 ＆ ログインアカウント発行フローチャート

## 1. 概要と問題の背景
従来の設計では、「新規入社手続きウィザード（手動登録）」および「台帳の従業員情報修正」に**メールアドレス入力欄が欠落**していました。
その結果、システムが自動生成した仮メールアドレス（`emp_...`）に対し、アカウント発行画面が勝手に `@company.local` という**実在しない架空のドメインを推測・捏造して表示**してしまい、現場の混乱と不信を招いていました。

本書では、この破綻していたフローを根本から是正し、**「入社登録 ➔ 台帳保存 ➔ アカウント発行 ➔ LINE案内 ➔ タイムカード打刻ログイン」**が一気通貫で矛盾なくつながる正常なデータフローを定義します。

---

## 2. フロー比較（Before vs After）

### ❌ 【Before】従来の破綻していたフロー（推測・捏造による混乱）
```mermaid
flowchart TD
    subgraph 管理者の操作
        A1["新規入社ウィザード（手動追加）<br>氏名・部署・給与を入力"] --> A2["❌ メールアドレス入力欄が存在しない！"]
        A2 --> A3["そのまま登録完了"]
    end

    subgraph システム内部・DB
        A3 --> B1["DB（usersテーブル）に保存<br>email: emp_1710000000@sample.local<br>（仮アドレスが自動生成）"]
    end

    subgraph ログイン案内
        B1 --> C1["台帳の「🔑 ログイン案内」ボタンを押す"]
        C1 --> C2["❌ アカウント発行モーダルが<br>safeName@company.local<br>（架空アドレスを勝手に捏造サジェスト！）"]
        C2 --> C3["LINE案内文に架空アドレスが記載<br>『test1@company.local』"]
        C3 --> C4["💥 スタッフはログインできない！<br>管理者『このメアドどこから取ってきたの！？』"]
    end

    style A2 fill:#ffebee,stroke:#c62828,stroke-width:2px
    style C2 fill:#ffebee,stroke:#c62828,stroke-width:2px
    style C4 fill:#ffebee,stroke:#c62828,stroke-width:2px
```

---

###  【After】今回の改修後の正常な統合フロー（推測ゼロ・実DB永続化）
```mermaid
flowchart TD
    subgraph パターン1: 管理者が手動追加する場合
        D1["新規入社ウィザード（手動追加）"] --> D2["【新設】メールアドレス入力欄<br>本人のGmailや社用メールを入力"]
        D2 --> D3["登録完了 ➔ DB（users）に本物のアドレスが保存"]
    end

    subgraph パターン2: 本人がスマホから入社手続きする場合
        E1["招待URLからスタッフ本人が入力"] --> E2["本人が自分のメールアドレスを入力"]
        E2 --> E3["書類提出 ➔ 管理者が承認<br>DB（users）に本物のアドレスが保存"]
    end

    subgraph 台帳での確認・修正
        D3 --> F1["従業員台帳（SSOT）"]
        E3 --> F1
        F2["【新設】従業員情報修正モーダル<br>いつでもメアドを変更・修正可能"] <--> F1
    end

    subgraph アカウント発行 ＆ タイムカード打刻
        F1 --> G1["台帳の「🔑 ログイン案内」ボタンをクリック"]
        G1 --> G2["【推測廃止】台帳の本物メアドが自動表示<br>※未登録の場合は空欄で入力案内"]
        G2 --> G3["「アカウントを発行してLINE案内文をコピー」をクリック"]
        G3 --> G4["① Supabase Authアカウント作成<br>② DB（users）のメールアドレスを即時永続化<br>③ LINE用案内文（URL・ID・初期パス）を自動コピー"]
        G4 --> G5["管理者がLINEでスタッフへ送信"]
        G5 --> G6["🎉 スタッフがスマホでURLを開き<br>本人のメールアドレスで1秒ログイン＆打刻！"]
    end

    style D2 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    style F2 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    style G2 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    style G6 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
```

---

## 3. 改修ポイント一覧

| 画面・コンポーネント | 従来の課題 | 今回の改修内容 |
| :--- | :--- | :--- |
| **新規入社ウィザード（STEP 1）**<br>`OnboardingAdminDashboard.tsx` | メールアドレス入力欄が無く、`emp_...` のダミーが生成されていた | **「メールアドレス（連絡先 兼 ログインID）」入力欄を新設**。<br>※本人の私用メール（Gmail等）または社用メールを入力可能に。 |
| **従業員情報修正モーダル**<br>`OnboardingAdminDashboard.tsx` | 既存従業員のメールアドレスを修正する手段が台帳上になかった | **「メールアドレス（ログインID）」入力欄を新設**。<br>保存時に `users` テーブルの `email` も同時に更新。 |
| **アカウント発行モーダル**<br>`StaffAccountIssueModal.tsx` | 仮メールアドレスの場合に勝手に `@company.local` を捏造していた | **架空ドメイン推測ロジックを完全撤廃**。<br>台帳にメールがあればそれを表示、未登録なら空欄にして入力案内。<br>発行時にDBへ本物アドレスを即時永続化。 |
| **LINE案内文・QRコード**<br>`StaffAccountIssueModal.tsx` | 不明瞭な文面 | タイムカード打刻URL、本人のメールアドレス、初期パスワード、ホーム画面追加のコツをワンタップで送信可能。 |
