# 【完全整理】従業員情報（氏名・フリガナ・メール・電話）登録 ＆ ログインアカウント発行フローチャート

## 1. 概要と情報整理の目的
新入社員の受け入れフローには、主に以下の2つの登録ルートが存在します：
1. **管理者による手動代行登録ルート**（新規入社手続きウィザード）
2. **専用URL送付による社員スマホ入力ルート**（専用入社URL発行 ➔ 社員スマホ手続き）

従来の設計では、以下の**情報欠落・非連動**が発生していました：
- 「専用入社URL発行」画面に**フリガナ・メールアドレス・電話番号**の入力欄がなく、氏名しか送れなかった。
- 社員側のスマホ入社手続き画面（`EmployeeOnboardingWelcome.tsx`）にも**メールアドレス入力欄が存在しなかった**。
- 手動追加ウィザードや台帳編集画面にもメール欄がなく、システムが仮アドレス（`emp_...`）を生成していた。
- その結果、アカウント発行画面が `@company.local` という**実在しない架空アドレスを捏造**してしまっていた。

今回の全面改修により、**「URL発行」「手動ウィザード」「社員スマホ入力」「台帳編集」「アカウント発行」の全画面で「氏名・フリガナ・メールアドレス・電話番号」が100%完全同期**し、矛盾のないクリーンなデータフローを確立しました。

---

## 2. 全体データフロー（完全統合版）

```mermaid
flowchart TD
    subgraph ルートA: 社員にスマホで入力してもらう場合
        A1["【管理者】個人別 労働条件・給与・役職設定 ＆ 専用入社URL発行<br>（氏名・フリガナ・メール・電話・給与・役職を設定）"] --> A2["専用URL発行（URLパラメータに全項目バインド）"]
        A2 --> A3["管理者がLINEやメールで社員へURLを送付"]
        A3 --> A4["【新入社員】スマホで入社手続き画面を開く<br>★管理者が設定した氏名・フリガナ・メール・電話が100%自動展開！"]
        A4 --> A5["社員が内容確認・住所や口座・証明書を入力して送信"]
        A5 --> A6["DB（users & employee_document_submissions）に即時同期保存"]
        A6 --> A7["【管理者】提出書類をワンクリック承認"]
    end

    subgraph ルートB: 管理者が手動で代行登録する場合
        B1["【管理者】新規入社手続きウィザード（手動登録）"] --> B2["【新設】氏名・フリガナ・メール・電話・給与・部署を入力"]
        B2 --> B3["登録完了 ➔ DB（users & profiles）に本物情報が保存"]
    end

    subgraph 大元マスタ（SSOT）: 従業員台帳
        A7 --> C1["🏢 従業員台帳（SSOT）<br>（本物の氏名・フリガナ・メール・電話・給与・役職が一元保持）"]
        B3 --> C1
        C2["✏️ 従業員情報修正モーダル<br>（台帳からいつでも氏名・フリガナ・メール・電話を変更可能）"] <--> C1
    end

    subgraph ログイン案内 ＆ タイムカード打刻
        C1 --> D1["台帳の「🔑 ログイン案内」ボタンをクリック"]
        D1 --> D2["【推測・架空アドレス完全撤廃】<br>台帳に保存された本物のメールアドレスが自動表示！<br>（未登録の場合は親切な入力案内バッジを表示）"]
        D2 --> D3["「アカウントを発行してLINE案内文をコピー」をクリック"]
        D3 --> D4["① Supabase Authアカウント作成<br>② DB（users）のメールアドレスを即時永続化<br>③ LINE用案内文（URL・ID・初期パス・ホーム画面追加のコツ）を自動コピー"]
        D4 --> D5["管理者がLINEでスタッフへ送信"]
        D5 --> D6["🎉 スタッフがスマホでURLを開き<br>自分の本物のメールアドレスで1秒ログイン＆打刻！"]
    end

    style A1 fill:#e3f2fd,stroke:#1565c0,stroke-width:2px
    style A4 fill:#e3f2fd,stroke:#1565c0,stroke-width:2px
    style B2 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    style C1 fill:#fff3e0,stroke:#e65100,stroke-width:2px
    style D2 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    style D6 fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
```

---

## 3. 全画面における情報整理・改修項目一覧

| 画面・機能 | 従来の課題 | 今回の改修内容（整理完了） |
| :--- | :--- | :--- |
| **個人別 労働条件設定 ＆ 専用入社URL発行**<br>`inviteUrlModal` | 氏名・給与のみで、**フリガナ・メール・電話が欠落**していた | **「氏名フリガナ（カタカナ変換付き）」「メールアドレス（打刻ログインID）」「電話番号」の入力欄を新設**。<br>URLパラメータにも全項目をバインド。 |
| **社員スマホ入社手続き画面**<br>`EmployeeOnboardingWelcome.tsx` | URLから氏名しか読めず、フォームにも**メール入力欄が無かった** | **URLからフリガナ・メール・電話を自動読取して初期展開**。<br>STEP 1に**「メールアドレス（連絡先 兼 ログインID）」入力欄を新設**。<br>提出時に `users` テーブルへも即時確定同期。 |
| **新規入社手続きウィザード（手動）**<br>`OnboardingAdminDashboard.tsx` | メール欄が無く、仮アドレス（`emp_...`）が生成されていた | STEP 1に**「メールアドレス（連絡先 兼 ログインID）」および「電話番号」入力枠を新設**。 |
| **従業員台帳 編集モーダル**<br>`OnboardingAdminDashboard.tsx` | 既存従業員のメールを台帳から直す手段がなかった | **「メールアドレス（ログインID）」「電話番号」入力枠を新設**。<br>保存時に `users` テーブルの `email` も同時に更新。 |
| **提出書類の承認処理**<br>`handleApproveSubmission` | 承認時にメールやフリガナの更新が不十分だった | 既存・新規問わず、社員が入力した最新の**氏名・フリガナ・メール・電話・生年月日・住所**を `users` テーブルへ100%確実に反映。 |
| **アカウント発行モーダル**<br>`StaffAccountIssueModal.tsx` | 仮メール時に勝手に `@company.local` を捏造していた | **架空ドメイン捏造コードを完全撤廃**。<br>台帳の実メアドを表示。未登録時は空欄＋案内表示とし、発行時にDBへ即時永続化。 |
